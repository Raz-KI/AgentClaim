[project]
name = "health-claims-system"
version = "0.1.0"
description = "Health claims processing service"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
